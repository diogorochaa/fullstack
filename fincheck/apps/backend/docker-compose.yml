version: '3'

services:
  postgres:
    image: postgres:latest
    container_name: fincheck-postgres
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
    ports:
      - '5432:5432'
